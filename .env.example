# Leave these unset to use the original localStorage test mode.
# The AWS deploy script writes .env.production automatically.
VITE_STORAGE_MODE=local
VITE_API_URL=
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_USER_POOL_CLIENT_ID=
